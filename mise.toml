[tools]
go = "1.25.5"
goreleaser = "2.17.0"
svu = "3.3.0"

[tasks.install]
description = "Download Go module deps"
run = "go mod download"

[tasks.fmt]
description = "Format Go packages"
run = "go fmt ./..."

[tasks.lint]
description = "Run go vet"
run = "go vet ./..."

[tasks.test]
description = "Run Go tests"
run = "go test ./..."

[tasks.build]
description = "Build rod-mcp"
run = "go build -o rod-mcp ./cmd/rod-mcp"

[tasks.ci]
description = "Format, vet, test, build"
depends = ["install", "fmt", "lint", "test", "build"]

[tasks.release]
description = "Tag and release with GoReleaser (svu + goreleaser)"
depends = ["ci"]
usage = '''
arg "<version>" help="New version" {
  choices "next" "major" "minor" "patch"
  default "next"
}
'''
run = '''
git tag $(svu ${usage_version})
goreleaser release --clean
'''
